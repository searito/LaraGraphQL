query createTransactionData {
    accounts (first: 20) {
        data {
            id
            name
        }
    }

    categories (first: 50) {
        data {
            id
            name
        }
    }
}
